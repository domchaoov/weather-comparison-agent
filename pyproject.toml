[project]
name = "weather-comparison-agent"
version = "0.1.0"
description = "Demo AI agent that compares weather between two places using local Ollama tool-calling."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ollama>=0.6.2",
    "overmind>=0.1.54",
    "python-dotenv>=1.2.2",
]
