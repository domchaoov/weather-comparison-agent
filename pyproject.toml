[project]
name = "weather-comparison-agent"
version = "0.1.0"
description = "Demo AI agent that compares weather between two places using OpenAI tool-calling."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openai>=2.45.0",
    "overmind>=0.1.53",
    "python-dotenv>=1.2.2",
]
